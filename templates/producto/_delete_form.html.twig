<form style="width: 31px; display: inline;" method="post" action="{{ path('producto_delete', {'id': producto.id}) }}"  onclick="return confirm('Are you sure you want to delete this item?');" id="form1">
    <input type="hidden" name="_method" value="DELETE"> 
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    {# <button class="btn">Delete</button> #}
     <a href="javascript:;" onclick="document.getElementById('form1').submit();"><i class="ik ik-trash-2 f-16 text-red"></i></a>
</form>
