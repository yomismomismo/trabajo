<!DOCTYPE html>

<title>New Pedidos</title>

{% block body %}
    <h1>Create new Pedidos</h1>

    {{ include('pedidos/_form.html.twig') }}

    <a href="{{ path('pedidos_index') }}">back to list</a>
{% endblock %}
